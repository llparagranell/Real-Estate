import express from "express";
import { SignupInput,SigninInput } from "../../validators/user.validator.js";

const router = express.Router();

// api/v1/user/auth/register
router.post('/register',(req,res)=>{
    validate
})